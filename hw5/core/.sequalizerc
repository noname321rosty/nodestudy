const path = require('path');

module.exports = {
    'config' : path.join(process.cwd(), ' configs' , 'sequelize.config.js'),
    'models-path' : path.join(process.cwd(), 'db' , 'models'),
    'seeders-path' : path.join(process.cwd(), ' db' , 'models'),
    'models-path' : path.join(process.cwd(), 'db' , 'models')
}
